[package]
name = "stylus-registry-smoke"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
hex = { version = "0.4.3", default-features = false }
tiny-keccak = { version = "2.0.2", features = ["keccak"] }
