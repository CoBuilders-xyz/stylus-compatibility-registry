[package]
name = "transitive-test"
version = "0.1.0"
edition = "2021"
[workspace]
[dependencies]
hex = { version = "0.4", default-features = false }
fs2 = "0.4"
transitive-libc = { path = "transitive-libc" }

[target.'cfg(windows)'.dependencies]
mio = "1"
