[package]
name = "stylus-compat-core"
description = "Core library for evaluating Rust crate compatibility with Arbitrum Stylus"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
cargo_toml = "0.22"

[dev-dependencies]
tempfile = "3"
